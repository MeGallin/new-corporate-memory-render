# Modernization implementation plan

This checklist keeps infrastructure changes, security fixes, API dependency upgrades, and client modernization in separate reviewable stages.

Last updated: 4 September 2026

## Current position

- Production API is running successfully as a Docker service on Render at commit `42422cd`.
- Render health checks are configured at `/health` and currently pass with MongoDB connected.
- Both Stage 2 security patches are deployed, including the admin-exposure, rate-limit, and focused route-test changes.
- The production API now has the Google web client ID required for verified Google sign-in.
- Render's GitHub App connection is repaired for the personal `MeGallin` account. The credential now includes `MeGallin/new-corporate-memory-render`, and Docker deployment `dep-dac4d5ijobas73cg1qq0` successfully made commit `02c494b` live. Repository clone, container build, MongoDB connection, reminder-cron startup, and Render `/health` probes all pass.
- The API now runs Mongoose 9.9.4 in production at `8bc7239`. Render is healthy, and the user verified create, edit, read, and delete behavior end to end; image behavior had already passed in an earlier production check.
- The API now uses Cloudinary 2.11.0 and Multer 2.3.0; the upgrade is verified locally and in production, including an authenticated image upload.
- The API now uses JSON Web Token 9.0.3 with HS256-only application token verification; the upgrade is verified locally and in production, including a manual login.
- The API now uses Express 5.2.1; the upgrade is verified locally and in production, including health, login, and missing-route checks.
- The API now uses Nodemailer 9.1.0 with hardened SMTP transport; the upgrade is verified locally and through an application email submission against the deployed backend.
- The LangChain/OpenAI integration upgrade and GPT-5.6 Luna default are deployed and verified through an authenticated Agent Chat request against production.
- Render has `OPENAI_MODEL=gpt-5.6-luna` and `OPENAI_EMBEDDING_MODEL=text-embedding-3-small`; the former misnamed `EMBEDDING_MODEL` key has been removed.
- The Agent Chat smoke request completed and returned a coherent answer from the user's memories. Follow-up client QA is deferred to Stage 5 by user decision because the component removes inline citation markers and does not render the returned citation metadata; the same page also displayed a zero-memory list while the agent found five memories.
- The Morgan 1.12.0, node-cron 4.6.0, and Moment 2.30.1 runtime group is deployed at `22d7d09`. Render verified container startup, MongoDB connectivity, Morgan health-request logging, and successful registration of the 08:00 Europe/London reminder task without manually firing it.
- Helmet 8.3.0 and CORS 2.8.6 are deployed at `288ef12`. Production verification confirmed `/health`, the allowed-origin and credential headers, omission of CORS permission for an unknown origin, Helmet's CSP and one-year HSTS header, and removal of `X-Powered-By`.
- bcryptjs 3.0.3 is deployed at `2273bfd`. The container and `/health` endpoint are healthy, and focused tests confirm existing `$2a$` hashes remain valid, new `$2b$` hashes retain 12 rounds, and new passwords cannot exceed bcrypt's 72-byte input limit. A credentialed production login was not independently performed for this dependency phase.
- dotenv 17.4.2 is deployed at `fb17c0f`. Render startup was clean, contained no dotenv injection messages, connected to MongoDB normally, and passed internal and public health checks.
- Google Auth Library 11.0.2 is deployed at `c34bce8`. Render starts normally, `/health` passes, and a deliberately invalid Google token returns HTTP 401. After correcting the ignored local API configuration, the user completed an ordinary Google sign-in successfully, closing the end-to-end QA check.
- The current production-install audit reports zero known vulnerabilities after refreshing MongoDB's compatible optional Socks dependency to 2.8.9.
- Atlas backup and access hardening remain deliberately deferred for their own controlled phase.
- The deployed Trilogy client is the modernized React 19/Vite build through client commit `b4e195c`. The visual redesign, validation, modal/dashboard/public-page refinements, Beta ribbon, persistent accent selector, Agent Chat citations, direct-refresh memory hydration, fluid shell, clearer API errors, and root-level Google provider are live. Local automated checks passed, and the user completed the final post-deployment manual verification successfully.
- Following user review, the redesign now has a strict four-colour lock: off-black, very dark blue, orange, and white. Every surface, border, muted label, semantic status, control state, and rating uses one of those four anchors or a calculated shade between them. The rejected cream/body background and the later circus-like collection of yellow, pink, green, gray, and blue accents have been removed. Header navigation now uses high-contrast white text. The user's fieldset wrapper remains the signature framing device across forms, memory cards, profile panels, and content sections. The Memories page now uses a compact control deck and responsive three/two/one-column masonry flow so image-heavy cards no longer create empty grid-row gaps. Its standard search and AI query are matching 175px labelled fieldsets in a two-column desktop row, collapsing to one column below 761px. Both use identical 46px inputs and aligned divider spacing. The search fieldset uses its available depth for a semantic `Memory status` fieldset containing the two larger active/completed metrics. The AI fieldset uses the natural label `Ask AI` and incorporates a `Due date` fieldset plus a bottom-aligned 54px Create button beneath its input. Latest and Soonest anchor to opposite sides of their available fieldset. Application behavior, routes, navigation labels, and form structure remain unchanged. Memory priority stars render in a dedicated horizontal inline-flex group instead of becoming separate children of the card's vertical flex layout.
- The user-specific Beta marker has been restored as a fixed orange top-left corner ribbon rotated at minus 45 degrees. It retains the approved palette, clears the first navigation control, ignores pointer input, and stays visible with a reduced footprint below 640px instead of disappearing. Desktop browser QA confirms the ribbon geometry and navigation clearance. The responsive CSS compiled successfully; the claimed browser tab did not accept the temporary viewport override, so the narrow variant remains for user visual confirmation. The client suite passes with 13 files and 22 tests, the Vite production build passes with 523 modules, and `git diff --check` passes.
- A second local theme-library review has been completed against the supplied design-system catalog and preview assets. `Construct - Spatial Engine` is the preferred primary reference because its compact dark workbench, nested operational panels, 8px geometry, and restrained technical hierarchy suit the internal application. `Mood Tracker Analytics` is useful only as a secondary reference for metric emphasis and selected orange states; its large mobile shells, 36px radii, and all-monospace body copy should not be copied. `Pricing Models` is a useful restraint reference, while the cinematic and WebGL-led themes are unsuitable for the daily-use client. The proposed next visual slice is a Memories-page workbench that keeps the four-colour palette and fieldset identity, unifies search and AI into a clearer command area, gives each memory a predictable metadata/content/action hierarchy, and retains the responsive masonry behavior for image-heavy records.
- The `Construct - Spatial Engine` Memories-page refinement is now implemented locally. A compact outer workbench introduces a clear page heading and total-memory indicator, with a 5/7 command grid above 1100px, equal query columns on tablet, and stacked panels below 761px. Query panels use scoped 8px geometry, flat dark-blue surfaces, restrained borders, and full-width equal Latest/Soonest buttons inside the `Filter due date` fieldset. The primary action is labelled `Create memory` and remains on one line down to 320px without overflow. The completed-memory status metric is now a semantic link to `#/user-admin`, where the existing dashboard renders the user's completed-memory cards. Completed cards label their checked state as `Unmark as complete`; active cards retain `Mark as complete`. A shared completion-feedback component gives state-specific success or error notifications on both the Memories and dashboard routes. The active-memory library has its own concise result header. Memory cards retain semantic fieldsets and masonry behavior while adding an unboxed priority row, a consolidated Memory controls fieldset, flatter image treatment, restrained card elevation, and a predictable hierarchy. Their visible fieldset title legends render in uppercase for consistency, while stored title values and accessible headings retain their original casing. When present, the rounded memory image sits directly beneath the main memory note. Created and Updated metadata follows as an unboxed text row, then Priority and the Memory controls fieldset. That fieldset contains the due-date and completion controls plus Edit, image upload/change, voice, and Delete on an unboxed action row separated by a single divider. The tag removal control and enlarged-image trigger are native labelled buttons, memory articles retain navigable heading names alongside their visible legends, count announcements no longer overlap, and shared buttons forward Agent Chat accessibility attributes. Desktop, 390px, and 320px browser checks pass without horizontal overflow or console warnings; Vitest, the Vite production build, `git diff --check`, the four-colour source audit, and the visible-source dash scan pass.
- The local dashboard now uses the accepted Memories workspace as its visual standard. `Dashboard workspace`, `Account overview`, and, when populated, `Completed memories` form compact shell sections with matching header rules, spacing, 8px geometry, dark surfaces, fieldset legends, and responsive behavior. The completed-memory section is omitted entirely when its count is zero. Account editing, profile image controls, memory metrics, account details, status, dates, and the administrator user selector have clear information hierarchy and consistent wording; the profile and editing fieldsets are labelled `Admin Profile Image` and `Edit Details`. Bare clickable profile icons and account values are now labelled buttons. Directly refreshing `#/user-admin` now reloads the authenticated user's details with the existing read-only request instead of leaving the account area blank. The expanded administrator selector now divides data into professional Identity, Access controls, and Activity cards with consistent label/value grids, readable Yes/No statuses, labelled toggles, and a black metric surface inside Memory summary. The two access controls use equal-width columns and collapse cleanly on narrow screens. The administrator selector's expanded state was browser-checked without changing user settings. Five Vitest files with eight tests, the production build, `git diff --check`, desktop browser QA, and a 390px responsive check pass.
- The local Memories modal system now follows the same workbench standard as the accepted page. Create and Edit use matching `Memory details` and `Reminder` fieldsets, labelled controls, concise supporting text, a consistent responsive grid, and clear primary actions. Delete uses a compact permanent-action confirmation, while image preview uses a dedicated media-sized shell with an accessible close control. The shared modal supplies dialog semantics, Escape handling, background scroll locking, focus restoration, consistent shell geometry, and compact/standard/media sizes. The React Datepicker surface, including its time input, now follows the four-colour theme. A collision with the global sticky-header rule was found during browser QA and removed by using scoped modal heading containers. Desktop checks of Create, Edit, Delete, the date picker, and image preview pass without mutating data; a 390px Create check confirms a single-column, internally scrollable dialog with no horizontal overflow. Six Vitest files with ten tests, the production build, `git diff --check`, the palette audit, and the visible-source dash scan pass.
- The local Contact route now follows the accepted workbench standard. A compact `Contact workspace` shell contains a focused two-panel layout: `Before you send` guidance and a structured `Send an enquiry` form. The former duplicated full About page has been removed from Contact while the independent About route remains unchanged. Name and Email share a balanced row, Message has an explicit label and live minimum-length guidance, and the primary action retains a stable `Send enquiry` label when disabled. Native required/minimum/description attributes now pass through the shared textarea component. The existing Redux/API submission flow is unchanged, and the success sentence and nine-character validation wording are corrected. Desktop and 390px browser checks pass without horizontal overflow; the mobile layout places the form first and guidance second. Seven Vitest files with eleven tests, the production build, `git diff --check`, palette scan, and visible-source dash scan pass.
- The remaining public entry surfaces now use the same documented workbench language. Home has a compact `Corporate memory` shell, paired Purpose/Memory model fieldsets, clear account/About actions, and a two-column knowledge-source library. About uses purposeful product, onboarding, AI, technology, protection, and Contact fieldsets instead of one long text slab. Both collapse to one column without horizontal overflow and keep the four-colour, 8px, fieldset-led system.
- The account and recovery surfaces now form one consistent `Account workspace`. Semantic Sign in, Create account, and Reset password buttons replace clickable text divs and expose their selected state. Login, registration, reset-link request, and token-based password reset share the same labelled fieldsets, concise headers, dark inputs, stable action wording, responsive grids, and full-width phone actions. Browser autofill is themed dark instead of rendering white credential fields. Existing Redux action payloads, Google login, routes, and authentication boundaries remain intact. Registration success now supplies its required reset callback, recovery copy is corrected, and password-reset redirection occurs only after API success with timer cleanup. Desktop and 390px browser checks covered all non-submitting states; ten Vitest files with fifteen tests and the production build pass.
- A full form-validation audit is now complete locally across public authentication and recovery, Contact, memory create/edit, account-detail editing, Agent Chat, and memory/profile image uploads. Shared client helpers now keep account, password, memory, priority, and image rules consistent. New passwords use the existing backward-compatible 6-to-72-character range plus uppercase, lowercase, number, and special-character requirements; sign-in checks only the minimum needed for existing credentials. Whitespace-only memory/contact/agent input is rejected, enabled reminders require a selected date, priorities remain within 1-5, AI filters accept only documented priority values, and image controls accept only JPG/PNG files no larger than 5 MB before preview. Inline errors are associated with their inputs, the password visibility control is keyboard accessible, and action labels remain stable while disabled. The API independently enforces the corresponding account, Contact, memory, and Agent Chat rules without changing existing stored records. Client tests pass with 12 files and 21 tests, API tests pass with 37 tests, the Vite production build passes with 523 modules, read-only browser validation QA passes, and a fresh-page console check is clear.
- Authenticated users now have a compact Dashboard accent-colour selector. Orange remains the initial default, with Yellow, Green, and Indian Red alternatives. The selection replaces only the existing accent token family, including its hover, soft, deep, focus, status, and rating derivatives; black, dark-blue, and white surfaces, typography, spacing, and fieldset structure remain fixed. Choices apply immediately throughout the application and persist per account in local browser storage. The most recently selected accent is also retained on public pages after logout, while signing into an account with its own saved choice restores that account's preference. Account identity is recovered from existing authenticated details or the JWT identifier. The selector is keyboard operable, reports its expanded and selected states, and closes with Escape. All four accent/button-text combinations exceed a 4.5:1 contrast ratio. Focused preference and selector tests pass, the full client suite passes with 15 files and 26 tests, and the Vite production build passes with 526 modules. Authenticated browser visual QA remains for the user because the currently open local session is signed out.
- All four API email flows now use one email-safe visual system that mirrors the accepted application: off-black canvas, dark-blue workbench card, white content, fixed orange accents, compact technical labels, 8px panels, and fieldset-like information blocks. Registration confirmation, password reset, contact acknowledgement, and seven-day reminders each have purpose-specific hierarchy and action wording while sharing the same branded shell and plain-text fallback. Table layout and inline styles avoid reliance on client-side CSS, JavaScript, external fonts, or remote images. Customer names, messages, and memory titles are escaped before HTML interpolation, multiline contact messages retain their formatting, and action links reject non-HTTP(S) protocols. Recipients, BCC handling, subjects, confirmation/reset tokens, reminder scheduling, and reminder state updates are unchanged. Gmail initially recoloured the navy as pale blue; commit `7d8fb9c` added legacy `bgcolor` attributes, same-colour gradient fallbacks, and a light-only colour-scheme declaration while retaining the exact approved palette. The corrected template is deployed, its final production Contact request returned HTTP 200, and the user confirmed the resulting Gmail message is acceptable. All 41 API tests pass.
- The final Agent Chat client gap is corrected and pushed on client `main` at `a393d04`. Inline `[M-<id>]` markers now become numbered links into a compact fieldset of returned memory titles and ranking scores; if the model omits inline markers, the first three API-ranked memories are labelled as relevant rather than referenced. The earlier zero-count mismatch came from direct-refresh sessions retaining authentication while lacking hydrated account details, so the Memories route now reloads account details before fetching memories, matching the established Dashboard behavior. A failed account-details request now presents a retry action instead of a misleading empty-memory state. The endpoint contract, authentication, ownership boundary, and database behavior are unchanged. Focused coverage passes, the full client suite passes with 16 files and 29 tests, and the React 19/Vite production build passes with 526 modules. Authenticated browser QA and Trilogy deployment remain pending.
- The Render GitHub App installation for the personal `MeGallin` account now includes `MeGallin/new-corporate-memory-render`. Deployment `dep-dac4d5ijobas73cg1qq0` successfully cloned and checked out email-template commit `02c494b`, built the existing Dockerfile, started the Node API, connected to MongoDB, enabled the reminder cron, and became live in 43.1 seconds. Render recorded two successful `GET /health` responses with HTTP 200. The earlier clone failure is resolved; no `GADev-equus` repository permission was changed.

### Live production QA findings — 2 September 2026

- The newly released client was inspected on the live authenticated Memories route without deleting or changing any existing memory. Public Home, About, Contact, and account-access routes render correctly; memory search, both due-date sorts, edit/cancel, delete/cancel, and image preview passed non-destructive checks.
- The completion and due-date toggle HTTP 400 defects are resolved in deployed API commit `42422cd`. Exact `isComplete` and `setDueDate` one-field payloads require real booleans, retain the ownership lookup, and change no other memory fields. Three route-level regression tests cover both contracts and malformed completion input; all 44 API tests pass, and the user confirmed the deployed behavior manually.
- The deployed application shell now uses fluid desktop width with the established gutters across content, header, user strip, and footer. Local checks measured approximately 1352px at a 1395px viewport and 363px at a 390px viewport, both without horizontal overflow; the user confirmed the deployed result manually.
- The Google Identity provider now mounts once at the application root rather than inside the route-level login component. This correction is deployed in client commit `b4e195c` and included in the successful manual release check.
- A clearly labelled production QA memory was created and isolated from existing data. Search, full edit, reminder creation, tag removal, image upload/preview/deletion, read-aloud, and Agent Chat with its linked Source 1 citation passed; the accent preference persisted across navigation and was restored to Orange. The QA image and record were permanently removed after explicit confirmation, and a clean reload verified the account had returned to its original 11 active/0 completed state with no browser warnings or errors. The subsequent corrected release was deployed and manually verified successfully by the user. No existing memory was changed or deleted.
- A plain-language customer handout is complete outside the two application repositories in `user-guide/`. The 14-page Word and PDF editions cover account creation and confirmation, sign-in and recovery, the Memory workspace, creation and card anatomy, edit/complete/restore/delete, images and read-aloud, search and Agent Chat, dashboard/profile/theme controls, logout, support, and a one-page cheat sheet. Four current application screenshots are included; remembered sign-in details are redacted. The final PDF was rendered and every page was visually inspected. The Word accessibility audit reports no high-severity findings and confirms that all four screenshots are inline with meaningful alternative text; its remaining table-header warnings are a known heuristic against layout-only step and callout tables.

### Next recommended action

The modernization and production-QA slice is complete. The first follow-on UI/UX refinements are complete locally: review the enlarged header labels and the mobile-first Memories card treatment, including the alternating dark/light card inversion, with the user, then commit and deploy only after explicit approval. Browser token storage remains the next technical review item when requested; Atlas backup and access hardening remain a later isolated phase.

## Stage 1 — Local Docker parity

- [x] Add an API Dockerfile that installs locked production dependencies and runs as a non-root user.
- [x] Exclude local secrets and development artifacts from the Docker build context.
- [x] Add a database-aware `/health` endpoint.
- [x] Add graceful HTTP, cron, and MongoDB shutdown handling.
- [x] Add a disposable MongoDB 8 local Compose environment.
- [x] Add a repeatable health smoke test.
- [ ] Build the image and run the smoke test locally. Render has successfully built and run the same Dockerfile; the local check remains blocked only by the Docker Desktop host fault.
- [x] Review the resulting source changes before production preparation.

## Stage 2 — Production safety and critical security

- [x] Verify Google identity tokens cryptographically using an API-side Google client ID.
- [x] Require authentication and ownership checks for memory-image uploads.
- [x] Derive profile-image ownership solely from the authenticated user.
- [x] Correct authentication middleware error propagation.
- [x] Add a 5 MB image limit, strict JPEG/PNG checks, and reliable temporary-file cleanup.
- [x] Add simple rate limits to public authentication and password-reset routes.
- [x] Limit the administrative user endpoint to required user fields and memory ownership references; do not load or return memory content.
- [x] Add focused built-in tests for bearer-token parsing, verified Google payloads, and ownership comparison.
- [x] Add focused route-level tests for rejected authentication, admin response exposure, rejected upload ownership, and authentication/recovery rate limits.

## Stage 3 — Backup and Render Docker conversion

- [ ] Create a timestamped `mongodump` archive from Atlas without changing database data.
- [ ] Verify the dump completed successfully and store it outside the repositories.
- [x] Record current Render settings and preserve all environment values.
- [x] Switch the existing Render service to its Docker runtime.
- [x] Configure `/health` as the Render health-check path.
- [x] Run production smoke checks and review Render logs.

## Stage 4 — API dependency modernization

- [x] Upgrade Mongoose 6 to Mongoose 8 and resolve deprecated connection and middleware behavior.
- [x] Deploy Mongoose 9.9.4. Production is healthy at `8bc7239`; create, edit, read, and delete checks passed, and image behavior had already been verified.
- [x] Upgrade Cloudinary and Multer in an isolated upload-focused change set to remove the legacy `vm2` path and Multer 1.x warnings.
- [x] Upgrade JSON Web Token 8 to 9 and pin application token verification to HS256.
- [x] Upgrade Express in its own controlled change set.
- [x] Upgrade Nodemailer and harden the SMTP transport.
- [x] Consolidate registration, password-reset, contact, and reminder emails into one branded, email-safe template system with escaped dynamic content and plain-text fallbacks.
- [x] Upgrade LangChain/OpenAI and move Agent Chat to the cost-sensitive GPT-5.6 Luna model; deployed at `1cdb0be` and verified with an authenticated production request.
- [x] Complete the final API dependency deployment. Morgan 1.12.0, node-cron 4.6.0, Moment 2.30.1, Socks 2.8.9, Helmet 8.3.0, CORS 2.8.6, bcryptjs 3.0.3, dotenv 17.4.2, Google Auth Library 11.0.2, and Mongoose 9.9.4 are complete in production.
- [x] Re-run the production-only security audit and resolve the currently reported reachable vulnerabilities; the local audit now reports zero findings.
- [ ] Move scheduled jobs out of the web process before adding more API instances.
- [ ] Avoid re-embedding every memory on every Agent Chat request.

## Stage 5 — Client modernization

- [x] Deploy the memory-delete confirmation correction as part of the cumulative Trilogy client release. The first click opens an application modal, Cancel performs no dispatch, and only a separate `Delete memory` button can invoke deletion; production lifecycle QA passed.
- [x] Render Agent Chat citation metadata and correct the direct-refresh memory-list/count mismatch. Inline source markers link to returned memory titles and ranking scores, while authenticated refreshes hydrate account details before loading memories and provide a retry state if that request fails. Deployed through client commit `b4e195c`; production Agent Chat returned the correct linked Source 1 citation and direct-refresh hydration passed release QA.
- [x] Replace the obsolete Create React App placeholder test with focused behavior coverage and migrate the runner to Vitest 4.1.11; the focused test passes after a clean install.
- [x] Migrate Create React App 5 to Vite 8.2.2 locally while retaining React 18, hash routing, port 3000, ignored mode-specific environment files, and the existing `build/` output directory. Both development and built-preview browser smoke checks passed without console errors; committed locally at `f93af6f`, with deployment pending.
- [x] Replace legacy Sass `@import`, global colour helpers, and duplicated stylesheet imports locally. Shared design tokens now live in a CSS-free Sass module, global styles load once, deprecated colour calls use `sass:color`, and the unused mixin file is removed. Tests and a warning-free production build pass; committed locally at `f9e152a`, with push and deployment pending.
- [x] Select and refine the visual redesign direction from the supplied references. The final user-directed system is constrained to four colour anchors: off-black, very dark blue, orange, and white. Ray of Light supplies the orange-on-black identity while Nexcore supplies the compact dashboard hierarchy and nested dark-blue surfaces. Avoid unrelated semantic colours, marketing-scale headings, WebGL/particle effects, and excessive animation.
- [x] Implement the visual redesign locally as a client-only slice. Central semantic tokens now derive every interface colour from the four approved anchors, while Space Grotesk and JetBrains Mono provide the typography. The shared system covers controls and states, application shell, high-contrast navigation, responsive Memories workspace, Agent Chat, image treatment, dashboard, forms, modal, footer, notices, and secondary views. The fieldset is the signature wrapper, including real fieldset/legend memory cards. The Memories page separates its two query modes into equal-width, equal-height desktop fieldsets: `Find a memory` retains live counts, while the more natural `Ask AI` fieldset incorporates the Due date segmented control and Create action beneath its input. All three legends share identical white typography and inset spacing; Due date and Create share the same 61px rendered height. The query layout collapses below 761px without horizontal overflow. Memory cards use natural heights and a responsive masonry flow with three columns above 1100px, two columns from 761-1100px, and one below 761px. Card media remains full-width above a compact action row ordered Edit, image action, voice action, Delete; the image and voice icons are now accessible 15/16px controls inside 34px buttons. Sort controls are keyboard-accessible buttons and priority stars are grouped horizontally. Focused Vitest, Vite production build, `git diff --check`, desktop/mobile browser QA, computed breakpoint verification, selected-sort interaction, modal review, header computed-style verification, and console review pass. User functional QA and explicit commit approval remain pending; proposed commit: `client: redesign application visual system`.
- [x] Refine the Memories page into the selected `Construct - Spatial Engine` workbench. The scoped slice adds the compact workbench and active-library headers, a wide-screen 5/7 query grid, flat 8px operational panels, full-width equal Latest/Soonest sort buttons inside a `Filter due date` fieldset, a `Create memory` action, 1.125rem main memory-note text, a rounded memory image directly beneath the note, an unboxed audit-date row, a vertically stacked unboxed Priority label and smaller star row, and a consolidated Memory controls fieldset containing state controls plus Edit, image, voice, and Delete actions. The four card-action positions use equal-width columns, eliminating the expanding middle gap; image change/delete controls share the image column when present, and narrow-card label padding is reduced below 421px. It also provides uppercase visible memory-card legends without changing stored or accessible title casing, restrained memory-card elevation, labelled tag-delete and image-view buttons, navigable memory headings, non-duplicated status announcements, and working Agent Chat disclosure attributes. It preserves the four-colour lock, existing behavior, component ownership, routes, fieldset signature, and three/two/one-column masonry. Vitest, production build, diff and palette checks, 1280px/390px/320px rendered checks, overflow checks, and console review pass; authenticated populated-card QA remains for the user.
- [x] Link the completed-memory status metric to the existing `#/user-admin` dashboard route. The link has a count-aware accessible name, keyboard focus treatment, and preserves the accepted status-panel appearance. Automated route coverage and a running-app browser check confirm the link opens the dashboard's Completed Memories section.
- [x] Make completion controls and notifications state-aware on both client routes. Completed cards say `Unmark as complete`, active cards say `Mark as complete`, and the Redux success state retains the requested boolean so the shared feedback component can distinguish marking from unmarking. Focused tests cover both messages and the completed-card dispatch, while read-only browser QA confirms the unmark control is visible on the dashboard without changing a memory.
- [x] Bring the dashboard route up to the accepted Memories workspace standard. Compact shell sections, consistent fieldset legends, an account-information grid, memory metrics, accessible edit/profile controls, conditional completed-memory output, two/one-column completed-card flow, and responsive breakpoints replace the former deeply nested and oversized layout. The completed-memory section is hidden when there are no completed records. Administrator selection and account details retain their existing behavior with clearer labels. Its expanded view uses Identity, Access controls, and Activity cards, consistent definition-list alignment, explicit Yes/No status values, accessible toggle names, and a black Memory summary metric surface. A direct-load hydration correction fetches missing user details for both ordinary and Google-authenticated sessions. Focused dashboard tests cover section output, direct-load hydration, and the zero-completed state; the full client suite, production build, diff check, authenticated desktop QA, expanded admin-panel QA, and 390px responsive review pass.
- [x] Carry the accepted workbench design through the public Contact route. The page now uses the same compact shell header, fieldset legends, four-colour surfaces, 8px geometry, spacing rhythm, and responsive rules as Memories and Dashboard. A focused guidance panel replaces duplicated About-page content beside the enquiry form. Inputs are explicitly labelled, the Message control exposes its minimum requirement, and the action retains meaningful wording in its disabled state. The original contact action and API request contract remain unchanged. Focused submission coverage, the full client suite, production build, desktop browser review, and 390px no-overflow review pass.
- [x] Carry the accepted workbench design through Home and About. Home now presents the purpose and model of corporate memory before a compact knowledge-source grid, with clear entry actions. About now separates product purpose, onboarding, AI behavior, technology, data protection, and Contact into consistent fieldsets. Both preserve their routes and core subject matter, use the four-colour/8px/fieldset system, and collapse to one column on phones without overflow.
- [x] Consolidate login, registration, forgotten-password, and password-reset surfaces into one responsive account-workspace system. The Forms route uses semantic mode buttons with selected-state attributes, a consistent access-guidance panel, dark autofill-safe controls, stable action labels, and a shared responsive form layout. Google login and existing Redux/API request contracts are preserved. Password-reset navigation now waits for successful completion and cleans up its timer. Focused workspace, mode-switching, and reset-payload tests pass alongside desktop and mobile browser QA of every non-submitting form state.
- [x] Restyle all Memories-route modals to the accepted workbench system. The shared modal shell now provides compact, standard, and media sizes; consistent black/dark-blue/orange/white styling; an always-visible labelled close control; dialog semantics; Escape dismissal; body-scroll locking; and focus restoration. Create and Edit share structured `Memory details` and `Reminder` fieldsets, real labels, matching spacing, responsive one/two-column control layouts, and clearer actions. Delete is a compact permanent-action confirmation, image preview uses the media shell, and the date picker is fully dark-themed. Browser QA covered every modal without submitting or deleting data, including 390px responsive behavior. Focused modal tests, the full client suite, the production build, diff/palette/dash checks, and manual accessibility inspection pass.
- [x] Align form validation across the client and API. Authentication, registration, password recovery/reset, Contact, memory create/edit, account-detail editing, Agent Chat, and image selection now apply matching field rules at the UI and request boundaries, expose specific inline feedback, preserve stable action labels, and retain API-side validation as the final authority. Existing login compatibility, authentication/ownership boundaries, stored records, and the 5 MB JPG/PNG upload contract remain intact.
- [x] Restore the Beta marker as the user's distinctive minus-45-degree orange corner ribbon, with non-blocking pointer behavior and a smaller responsive treatment.
- [x] Add an authenticated Dashboard accent-colour selector with Orange, Yellow, Green, and Indian Red variants. The runtime token layer changes the complete accent family without altering the accepted dark surfaces or fieldset-led layout, stores the choice per account, and retains the last selected accent on signed-out public pages in the same browser.
- [x] Upgrade React Router 6 to 7 with the React 19 dependency phase. React Router 7.18.3 retains the existing declarative `HashRouter` behavior, removes the two remaining router audit findings, and is deployed with authenticated navigation verified.
- [x] Upgrade React 18 to React 19 and update incompatible dependencies. React 19.2.8, React DOM 19.2.8, React Redux 9.3.0, Redux 5.0.1, Redux Thunk 3.1.0, React Datepicker 9.1.0, current Testing Library packages, and the maintained Redux DevTools extension install cleanly with zero audit findings. The React 19/Vite client is deployed through `b4e195c`, with post-deployment manual QA successful.
- [ ] Reassess browser token storage and authentication handling.
- [x] Build and verify the static production client and deploy it to Trilogy hosting; the user completed the final post-deployment manual check successfully.
- [x] Complete the first follow-on UI/UX refinement locally. The application header now uses a restrained three-level typographic wordmark: small tracked mono `YOUR`, steady uppercase `CORPORATE`, and larger bold `MEMORY` with a short standard-white underline. A transparent content-fit surface with balanced padding lets the wordmark and its approved supporting line sit directly on the established dark header. Instead of a continuous visible frame, two short 3px accent strokes trace only its top-left and bottom-right 8px corners; the remaining border stays visually absent while retaining the established geometry. The identity is anchored at the normal left content gutter, uses 68px of the 76px desktop header row, and sizes itself to its enlarged typography instead of reserving an arbitrary fixed width. It measures approximately 267px at desktop and 218px at tablet width, with equal zero outer padding on its left and right sides. A responsive grid gutter preserves the visual interval from the identity to Home while the four navigation columns narrow evenly: measured whitespace is approximately 172px beside the identity and 172px between comparable desktop labels, scaling to approximately 82px and 80px at tablet width. The active navigation state keeps its 3px accent underline and now rounds only its bottom corners to the shared 8px control radius; its top edge remains square. The wordmark itself is unaffected; the diagonal Beta ribbon is mirrored into the upper-right corner. `MEMORY` and the two corner strokes use the runtime accent variable, so Orange, Yellow, Green, and Indian Red preferences are inherited automatically while the underline stays neutral white. Two-level navigation labels, the clearer authenticated account strip, and matching Logout treatment remain. The rejected provisional YCM symbol is absent. Existing routes, dark four-colour system, and a sub-80px desktop height are retained. On the account-access route, Sign in and Google sign-in share one equal-width desktop action row and collapse to equal full-width controls on phones. Browser checks at 1280px, 820px, and 390px found no clipping or horizontal overflow, and confirmed that the right-corner Beta marker does not obscure the Login label; the full 16-file/29-test Vitest suite, Vite production build, `git diff --check`, and visible-copy dash audit pass. User review, commit, deployment, and production QA remain pending.
- [x] Refine the shared header and page-shell spacing. The header's lower rule is removed so the shell ends cleanly beneath navigation. The accepted wordmark keeps its transparent background, while the active navigation state retains a 3px accent edge with only bottom-corner rounding. The shared `.content-wrapper` top padding is reduced from the former 20-40px desktop range to 12-20px and from 16px to 12px on phones, moving every route upward consistently while preserving internal page spacing and footer clearance. Browser checks across Home, Memories, About, Contact, Forms, User Admin redirection, and Password Reset confirmed the shared offset, no horizontal overflow, a 0px header bottom border, and the expected bottom-only active radius at desktop; a 390px Contact check confirmed 12px top padding and no overflow. User review, commit, deployment, and production QA remain pending.
- [x] Increase the primary header navigation type scale without changing the supporting labels. Home, About, Contact, Login, and the authenticated Logout action now use 0.95rem desktop text and 0.8rem phone text; secondary navigation lines remain unchanged at 0.55rem desktop and stay hidden on phones. Desktop and 390px browser checks confirm the four labels remain readable, fit within the navigation grid, preserve bottom-only active rounding, and produce no horizontal overflow. User review, commit, deployment, and production QA remain pending.
- [x] Rework the Memories route for mobile-first readability. Below 761px, cards keep the accepted dark four-colour and fieldset system but use a clearer hierarchy: larger card titles and notes, a subtle accent edge and dark note surface, larger date and priority metadata, more legible state controls, and a two-row action grid with full-width Edit, image, voice, and Delete buttons. The workbench and query/status controls also receive a modest phone type-scale lift without changing desktop layout or supporting navigation labels. At 420px and below, metadata stacks to one column and action labels remain readable; the status metrics switch to a single column at 380px and below. Browser checks at 390px, 380px, and 320px confirm the single-column flow, no horizontal overflow, and usable control widths; Vitest, the production build, and diff checks pass. User review, commit, deployment, and production QA remain pending.
- [x] Add restrained alternating Memory card surfaces. Every even card now uses a slightly deeper shade derived from the existing dark-blue and off-black tokens, while odd cards retain the standard surface. The alternating treatment is scoped to the rendered collection wrappers, preserves the fieldset geometry, borders, text contrast, and four-colour lock, and requires no data or component-logic changes. Desktop, 390px, and 320px browser checks confirm the alternating computed backgrounds, usable card widths, and no horizontal overflow; browser warning/error logs are empty. User review, commit, deployment, and production QA remain pending.
- [x] Refine the alternating Memories treatment into the requested inverted variant. Odd cards retain the dark-blue surface with light writing, while every even card uses the approved off-white surface with dark writing, light control trays, and the same border hierarchy. Orange remains reserved for due urgency, ratings, focus, and actions. The Memories collection measures each card's rendered masonry position and applies a visual-order tone attribute, so the left-to-right/top-to-bottom sequence alternates at desktop and tablet while the single-column sequence alternates on phones; this small client-side measurement leaves stored data and card behavior unchanged. Desktop, 390px, and 320px browser checks confirm the alternating surfaces, readable text/control contrast, usable card widths, and no horizontal overflow; browser warning/error logs are empty. User review, commit, deployment, and production QA remain pending.
- [x] Make the inverted alternate-card treatment a fixed Memories rule across responsive breakpoints. The global rendered-card selector is intentionally outside the mobile media query, so desktop, tablet, and phone collections all alternate dark-blue and off-white cards; mobile-only rules adjust spacing and note surfaces without changing the alternating contract. Because the accepted desktop layout is balanced CSS masonry, the sequence flows top-to-bottom within columns, so the first visual row can contain the odd dark cards while the next visual row contains the even light cards; this is layout flow, not a mobile-only exception. Memory legends now use small title plates tied to their card surface: the dark cards use the existing dark-blue token, while light cards use the existing off-white token. Both title plates use a contained child visual whose top/left/right edge is limited to the upper half of the plate, so its vertical strokes terminate at the card's horizontal fieldset edge instead of continuing into the card. The visible legends on each light card, including the nested `Memory controls` legend, use the current runtime accent token, keeping Orange, Yellow, Green, or Indian Red identifiable against the off-white surface. Breakpoint checks at desktop and tablet widths confirm the same sequence and no overflow. User review, commit, deployment, and production QA remain pending.
- [x] Create a customer-ready simple user guide in editable Word and print-ready PDF formats. The guide follows the current application wording and validation rules, uses the accepted navy/orange/off-white visual language, includes redacted current screenshots, and provides numbered task instructions plus a one-page quick-reference sheet. All 14 rendered pages have been visually checked; no application code or production data was changed.
- [x] Clarify and visually align the public account-access experience, with the mobile sign-in journey as the priority. The route opens with `Sign in to your memories`, places the active form before secondary choices, removes the redundant right-side task label, and gives the native Sign in action the full available width. Google follows beneath a dedicated `Or continue with` divider instead of competing for space in the same row. Only the two alternative account tasks appear below, inside the established uppercase-legend fieldset pattern. Their controls now use the existing runtime accent as a clear outline and pressed edge over a distinct dark-blue surface, so they read as buttons while remaining secondary to Sign in. Create-account and password-recovery modes use matching task-specific headings and the same form-first order. The public navigation label is consistently `Sign in` rather than `Login`, and reassurance copy is shorter on phones. Authentication actions, request payloads, Google sign-in, and routes are unchanged. The reusable review criteria are recorded in `docs/UI-UX-AUDIT-CHECKLIST.md` for later route-by-route audits. Browser checks covered all three modes and the refined Sign in view at the narrow active viewport; the full 17-file/31-test client suite, production build, and `git diff --check` pass. User review, commit, deployment, and production QA remain pending.
- [x] Refine the desktop account-access composition after the full-width audit. The guidance fieldset now uses its natural content height rather than stretching to the combined height of the Sign in and Other account options panels. Its legend is the distinct `Account guidance`, the duplicated privacy sentence and decorative `Welcome back` heading are removed, and its supporting descriptions use the established stronger off-white text token. The form-first mobile order, clear alternate-action buttons, fieldset identity, authentication behaviour, and Google control's supported 400px maximum remain unchanged. Desktop and narrow browser review, focused tests, the full client suite, the production build, and `git diff --check` pass; user review, commit, deployment, and production QA remain pending.
- [x] Use one task-first account-access order at every breakpoint. The active form and its alternate account actions now precede Account guidance in the actual document structure, so desktop presentation, phone presentation, keyboard navigation, and assistive technology all encounter the call to action first. The desktop grid gives the working column the wider seven-part span and guidance the narrower four-part span. The former mobile-only grid-row override is removed because CSS no longer needs to repair a different source order. Existing actions, validation, authentication, responsive stacking, and guidance content are unchanged; user review, commit, deployment, and production QA remain pending.
- [x] Compact the Sign in action area on desktop without weakening its hierarchy. The native Sign in action and the supported Google control now share one row with a concise `Or continue with` separator between them, while the existing 760px breakpoint restores the clearer stacked layout and ruled separator on phones. Both actions retain equal available columns, their real clickable bounds, and the established minimum height. Authentication behaviour, validation, provider integration, and alternate account actions are unchanged; user review, commit, deployment, and production QA remain pending.
- [x] Establish unmistakable editable-control affordance across the client. A shared Sass treatment now gives text inputs, password and email fields, searches, text areas, the administrator selector, Agent filters, and date/time controls a two-pixel neutral border, persistent restrained accent edge, deliberately quiet 25% placeholder, and stronger hover and focus response. The lower placeholder prominence prevents hints from resembling existing values while persistent field geometry still communicates editability. A final structured review removed the overridden per-component appearance declarations, moved the shared radius into the mixin, and retained the focus ring and accent edge for browser-autofilled credentials. Disabled, invalid, entered, checkbox, toggle, and custom file-upload behaviour remains owned by the existing specialised components. Account-mode labels, headings, and component selection now come from one module-level configuration rather than parallel arrays and conditional rendering. The rule is applied through the existing component styles rather than a broad selector that could affect non-form controls; user review, commit, deployment, and production QA remain pending.
- [x] Complete the first Memories workspace UX refinement. The wordmark remains deliberately hidden below 760px, matching the established mobile behavior, while the authenticated navigation retains a clear one-line 54px interaction row. Removal of the duplicate workspace total prevents the fixed Beta ribbon from competing with page status. Create memory is an independent primary action in the workspace heading. Find a Memory and account-level Memory Status remain permanently visible; the concise Active and Completed metrics stay side by side at narrow widths, and zero completed memories render as non-interactive status. Latest/Soonest due-date sorting and Ask AI now share one closed-by-default fieldset whose legend is `Fine-tune results`; its internal action row retains a complete control radius and border across default, hover, focus, and open states, uses a prominent double-sized plus/minus indicator, and expands both tools together only when needed. Within the expanded tools, Filter Due Date uses the same internal fieldset padding as Ask AI, its Latest/Soonest controls match the AI input's 46px height, and the pair aligns with the full left and right edges of the action row above. The library removes its third duplicate loaded count, while loading and failure status remain visible. Explicit empty states now cover an account with only completed memories and a search whose matches are completed. The accepted desktop wordmark, palette, responsive alternating cards, fieldsets, routes, authentication, memory actions, data, and API contracts are unchanged. Focused tests, the full 17-file/35-test client suite, the Vite production build, authenticated desktop and narrow review of collapsed and expanded disclosure states, and diff checks pass; user review, commit, deployment, and production phone QA remain pending.
- [x] Improve the boundary between consecutive memory cards on screens below 760px. The accepted alternating dark/light surfaces remain unchanged, while the mobile-only inter-card gap increases from 16px to 28px. A full-width four-pixel tapered separator now peaks in the active accent colour at its centre, fades to transparent at both edges, and uses a restrained token-colour glow between consecutive cards. The final card retains the established trailing spacing. Desktop spacing and all card content and behaviour remain unchanged. Focused tests, the production build, mobile browser review, and diff checks pass, and the user has accepted the resulting experience; commit, deployment, and production phone QA remain pending.
- [x] Complete a structured review of the uncommitted Memories slice only. The review covered correctness, authentication and authorization boundaries, sensitive-data exposure, error handling, client compatibility, dependency and migration risk, expected-use performance, responsive accessibility, test isolation, documentation, and unintended scope. Actionable findings were resolved: active/completed totals now share one defensive array pass, masonry-tone resize work is coalesced to at most one animation frame and cancelled during cleanup, retry controls explicitly use `type="button"`, and a regression test confirms search does not alter account-level status totals. The final scope remains four client files plus the implementation plan and UX checklist, with no dependency, API, persistence, secret, or production-data changes. The full 17-file/36-test suite, production build, authenticated narrow runtime review, changed-file secret/conflict/whitespace scan, and client/API diff checks pass. Real production-phone QA, assistive-technology testing, and high-volume memory benchmarking remain unverified; commit and deployment remain pending.
- [x] Extend the authenticated accent selector with Pink (`#e66a9f`) and Purple (`#9b7de5`) while retaining Orange, Yellow, Green, and Indian Red. The six choices use the existing token-driven accent system, per-account local-browser persistence, and unchanged dark/off-white surfaces. Pink and Purple achieve 6.50:1 and 6.06:1 contrast respectively against the standard dark action text. The dropdown is now a compact fieldset containing a label-free three-by-two swatch grid: each full-size grid cell is the click target, thin shared dividers create the requested checkerboard structure, round colour samples remain visually clear, and the selected swatch has a visible ring. Colour names remain available to assistive technology and native tooltips without adding visible text. Selecting a colour applies it and closes the palette immediately. Dashboard stacking keeps the palette fully visible above the following account section. Focused preference/component tests, full client tests, production build, and authenticated browser selection, automatic-close, and refresh-persistence checks pass; the original Yellow preference used for QA was restored. Commit, deployment, and production QA remain pending.
- [x] Expand the accent palette from six to ten choices with four deliberately bright alternatives: Electric Blue (`#5aa9ff`), Cyan (`#35c6d0`), Lime (`#9bd65c`), and Coral (`#ff8a65`). Their contrast against the established near-black action text ranges from 8.03:1 to 11.42:1. The swatch fieldset adapts to a compact five-by-two checkerboard at desktop and retains the same two-row structure at phone widths with smaller but usable samples. A derived same-hue `accent-on-light` token darkens accent typography only on inverted off-white Memory cards, keeping all ten title and fieldset legends at or above 4.5:1 without changing the selected bright accent elsewhere. Accessible names, tooltips, selected rings, local-browser persistence, immediate application, and automatic closure are unchanged. Focused tests, full client tests, production build, the contrast audit, ten-swatch authenticated browser review, and Cyan review across dark and inverted Memory cards pass; the starting Purple preference was restored. Commit, deployment, and production QA remain pending.
- [x] Complete a structured review of the uncommitted ten-colour accent slice. The review covered correctness, account-scoped preference behaviour, authentication and authorization boundaries, sensitive-data exposure, error handling, current-client compatibility, dependency and migration risk, expected-use performance, responsive accessibility, test coverage, and unintended scope. Actionable findings were resolved: internal swatch dividers now use the grid gap instead of selectors tied to exactly five columns, closing by selection or Escape returns keyboard focus to the trigger, the selector honours reduced-motion preferences, and a duplicated trigger style block was consolidated. No API behaviour, database records, authentication rules, dependencies, environment configuration, or external systems changed. The final 17-file/38-test suite, Vite production build, client/API diff checks, changed-file secret scan, authenticated ten-swatch visual review, and live Escape/focus check pass. Real production-device and screen-reader QA remain unverified; commit and deployment remain pending.
- [x] Create a standalone comparison for the next Memory-card internal-layout decision without changing the working card component. `client/public/prototypes/memory-card-layout-options.html` presents three responsive, fieldset-led alternatives using the accepted title plate, single active accent, and dark/light card treatments: the recommended Reading first hierarchy, a Split workspace, and a Task-led footer. Every option preserves the existing note, due date, tag, priority, created/updated dates, due-date state, completion state, Edit, image, read-aloud, and Delete capabilities while changing their visual order and grouping. The page includes a dark/light sample switch and plain-language trade-off notes. Browser review confirms all three desktop concepts and both surface treatments; DOM parsing, forbidden-dash scan, and diff check pass. The application route, component behaviour, data, API, and production systems remain unchanged. User selection and phone-width visual review are pending before implementation.
- [x] Enhance the same Memory-card comparison page through the requested Taste-skill redesign-preserve audit. Each concept now labels the primary content as `Memory note`, presents priority and dates as a sparse semantic definition list instead of three boxed mini-cards, separates `Status` from `Actions`, and gives Edit unmistakable primary-action hierarchy with a concise `Open details` explanation. Secondary actions use clear text labels, destructive intent remains outlined, and focus, active, and reduced-motion states are explicit. The comparison framing was simplified to vertically stacked headings, excess option eyebrows were removed, and the recommended concept receives one restrained accent edge rather than decorative effects. Authenticated application code remains untouched. Desktop browser review confirms the refined dark and light treatments; semantic structure checks, forbidden-pattern scan, production build, generated-prototype check, and diff checks pass. User selection and phone-width visual review remain pending.
- [x] Replace the first Memory-card prototypes after user review found their hierarchy too faithful to the existing component. The rebuilt standalone page now starts from the non-technical user's task sequence: identify the memory, read its note, judge urgency, take the likely action, then reveal supporting details. It compares two purposefully phone-width patterns rather than three control rearrangements. `Recall, then act` leads with the note and keeps Mark complete and Edit visible, while `Urgency, then recall` gives the deadline first position for task-heavy use. Created/updated history, reminder configuration, image management, read-aloud, and Delete move behind a native `Details and more actions` disclosure; Delete is separated as a destructive action instead of competing with routine controls. The established fieldset/title treatment, dark and off-white samples, single runtime-style accent, typography, and 8px geometry remain. Application components, routes, behavior, API, persistence, and production systems are unchanged. Dark/light browser review and disclosure interaction checks pass; user selection remains pending before implementation.
- [x] Record the user's selection of `Recall, then act` as the preferred Memory-card direction. Increase only its due-date signal from 0.79rem to 1rem so urgency uses the available status-row space more clearly while the accepted note, priority, actions, disclosure, spacing, and alternate study remain unchanged. Implementation in the working Memory component remains pending until separately requested.
- [x] Refine the selected prototype's expanded action layout. Change image and Read aloud retain an equal 50/50 first row, while Reminder settings and Delete memory each occupy a separate full-width row. The same disclosure pattern is shown in both comparison examples for component consistency; no application implementation has been made.
- [x] Clarify the prototype-only wrapper boundary. `.phone-stage` exists solely to constrain each comparison sample to a visible 390px mobile canvas; it is not part of the selected Memory-card design and must not be carried into the application. The real component should retain only its existing collection wrapper and the signature Memory fieldset.
- [x] Implement the selected `Recall, then act` hierarchy in the real Memory card using the established component classes, Sass tokens, fieldset title treatment, alternating dark/off-white surfaces, 8px controls, and responsive breakpoints. Title/tag and the note lead, optional media remains directly below the note, and a 1rem due-date signal shares one sparse status row with Priority. State-aware Mark complete/Unmark and Edit are the only always-visible actions. A native closed-by-default `Details and more actions` disclosure contains Created/Updated history, labelled Add/Change image and Read aloud controls, the existing due-date-enabled setting, and a separate full-width Delete action. Image management keeps file validation, preview/upload, and image deletion, now behind one labelled action; when the image selector expands, Read aloud also spans the row so no empty grid cell remains. Existing Redux actions, ownership boundaries, modals, confirmation flows, speech, image preview, and API contracts are unchanged. Focused card/image tests, the full 18-file/41-test suite, Vite production build, diff/secret/conflict/dash checks, and authenticated browser review of dark and inverted cards, collapsed/expanded details, and expanded image selection pass. Real phone and assistive-technology QA remain pending; commit and deployment remain pending.
- [x] Match the Memory card's `Details and more actions` disclosure indicator to the established `Fine-tune results` control. Both plus/minus indicators now use the same 2.5rem size and line height; card content, spacing, disclosure behavior, and responsive layout are unchanged.
- [x] Separate destructive intent from the selectable accent system. The existing danger role now resolves to a permanent Indian Red (`#cd5c5c`). Delete memory, its final confirmation, Delete image, Delete profile image, and Delete account/user use a transparent button with a three-pixel double red border and red text under every accent choice; on hover they fill with a deeper fixed red and switch text/icons to the existing off-white at 5.3:1 contrast. The compact tag-delete control uses only a red `X` without a fill or border. The Forms selected-state underline was returned to the active accent, inverted-card tag styling uses the dedicated accent-on-light token, and the two non-destructive image-preview cancellation buttons now use the neutral secondary treatment instead of danger. The full client suite and production build pass, and authenticated browser review under the Lime accent confirms deletion remains visibly red while personalised actions remain Lime.
- [x] Make Memory deletion confirmation identify the selected record before the irreversible action. The existing confirmation modal now presents the Memory title and full note first, followed by Created and Updated dates plus the exact due date and relative due/overdue status when applicable. Generic filler is replaced by a concise recovery warning, while Cancel and the final Delete Memory action remain visually and behaviorally distinct. Its complete outer frame and top status edge use the permanent Indian Red danger token rather than the user's selectable accent; ordinary modals retain their personalised accent. The client passes the already-loaded Memory object into the confirmation component; the dispatched delete request remains ID-only, with no API, authorization, persistence, or data-contract changes. Focused deletion/card/modal tests, the full 18-file/41-test suite, the Vite production build, `git diff --check`, and authenticated browser review of both dated and undated records pass. Browser confirmations were cancelled and no Memory was changed; commit, deployment, and production QA remain pending.
- [x] Close the confirmed image-cleanup gap in full Memory deletion. Full Memory deletion and standalone image deletion now use one tested Cloudinary cleanup helper. After the existing owner-scoped lookup, a Memory with an image destroys the Cloudinary asset before its MongoDB document is deleted; a record with no image skips the provider call. Cloudinary `not found` is treated as already clean, while provider exceptions or unsuccessful responses prevent database deletion and successful reporting so the operation remains retryable. A record containing an image URL without its cleanup identifier is also retained rather than knowingly orphaning the remote asset. Existing routes, request/response contracts, authentication, authorization, models, dependencies, and client behavior are unchanged. Nine focused tests cover cleanup success, already-absent assets, no-image records, provider failure, missing identifiers, ownership/not-found rejection, operation ordering, and standalone image-field cleanup. The complete API suite passes with 53 tests and `git diff --check` is clear. A controlled provider smoke test uploaded a uniquely named one-pixel temporary asset and deleted it through the new helper; Cloudinary returned `ok`. No existing asset or database record was touched. The visible local end-to-end smoke test is recorded below; deployment and production smoke testing remain pending.
- [x] Complete the visible local end-to-end image-deletion walkthrough. Through the authenticated Memories interface, created the disposable `Cloudinary deletion walkthrough` Memory, uploaded the existing non-sensitive client sample image, confirmed the card and image rendered, and verified the stored Cloudinary asset existed before deletion. The red confirmation modal displayed the correct title, full note, Created/Updated dates, and irreversible warning. Confirming deletion produced the success notification and returned the active count from seven to six. The disposable card disappeared, its MongoDB record was absent, and a read-only Cloudinary resource lookup returned 404 for its exact public identifier. The existing `Cats` image Memory remained present and was not changed. The complete API suite still passes with 53 tests and both repository diff checks pass. No commit, deployment, or production-system change was made; deployment and production smoke testing remain pending.
- [x] Complete a full API and client code review of the current cumulative change sets. Correctness and resilience fixes now enforce confirmed/non-suspended accounts at the API boundary, use 403 for authenticated authorization failures, validate administrator toggles as booleans, prevent registration-confirmation and password-reset tokens from appearing in request logs, normalize email addresses consistently, return explicit password/reset-token-free user representations, avoid account enumeration during password recovery, and expire confirmation links after 24 hours. Agent filters validate every documented input, empty accounts avoid unnecessary OpenAI calls, and stored Memory excerpts are explicitly treated as untrusted prompt data. The reminder job now compares its string-backed due dates correctly in application code and skips unconfirmed or suspended recipients. Shared Cloudinary upload/replacement/deletion utilities remove duplication across Memory/profile flows, clean a new upload after a failed database save, treat failed old-image cleanup as a non-fatal warning after a successful replacement, and include profile and Memory assets in administrator account deletion. Client fixes repair the profile-image delete route, preserve failed sign-in input, surface Google/API failures consistently through one error helper, recover safely from corrupt stored authentication JSON, clean up the DateTime interval, guard unavailable read-aloud support, remove ineffective asynchronous/action cleanup code, and trap keyboard focus inside shared modals. Dead imports and stale route comments were removed without moving established modules or changing persistence schemas. Direct verification passes with 62 API tests, 20 client test files/47 tests, the Vite production build, JavaScript syntax checks, both production-dependency audits at zero known vulnerabilities, both diff checks, and changed-file secret/conflict scans. No database, environment, external-service, dependency-version, commit, deployment, or production-data change was made. Real Google/email/provider failure paths and production smoke testing remain pending. Moving browser tokens from local storage to secure HttpOnly cookies remains a separately planned authentication migration because it changes the API/client session contract.
- [ ] Continue the Memories refinement only after review of this implemented slice. Remaining candidates are to replace ambiguous `Filter due date` / `Latest` wording if the user approves and reassess desktop CSS-column masonry because its visual scan order differs from document order and adds fragile position-measurement logic.

## Stage 6 — Atlas hardening

- [ ] Create a least-privilege database user for the application.
- [ ] Restrict the Atlas IP access list to the required Render and administrative source ranges.
- [ ] Decide whether to retain manual dumps or upgrade Atlas for managed backups.
- [ ] Do not combine Atlas access changes with the initial Render runtime conversion.
