# Modernization implementation plan

This checklist keeps infrastructure changes, security fixes, API dependency upgrades, and client modernization in separate reviewable stages.

Last updated: 2 September 2026

## Current position

- Production API is running successfully as a Docker service on Render at commit `8bc7239`.
- Render health checks are configured at `/health` and currently pass with MongoDB connected.
- Both Stage 2 security patches are deployed, including the admin-exposure, rate-limit, and focused route-test changes.
- The production API now has the Google web client ID required for verified Google sign-in.
- Render's GitHub App connection is repaired for the personal `MeGallin` account. The credential now includes `MeGallin/new-corporate-memory-render`, and Docker deployment `dep-dac4d5ijobas73cg1qq0` successfully made commit `02c494b` live. Repository clone, container build, MongoDB connection, reminder-cron startup, and Render `/health` probes all pass.
- The API now runs Mongoose 9.9.4 in production at `8bc7239`. Render is healthy, and the user verified create, edit, read, and delete behavior end to end; image behavior had already passed in an earlier production check.
- The API now uses Cloudinary 2.11.0 and Multer 2.3.0; the upgrade is verified locally and in production, including an authenticated image upload.
- The API now uses JSON Web Token 9.0.3 with HS256-only application token verification; the upgrade is verified locally and in production, including a manual login.
- The API now uses Express 5.2.1; the upgrade is verified locally and in production, including health, login, and missing-route checks.
- The API now uses Nodemailer 9.1.0 with hardened SMTP transport; the upgrade is verified locally and through an application email submission against the deployed backend.
- The LangChain/OpenAI integration upgrade and GPT-5.6 Luna default are deployed and verified through an authenticated Agent Chat request against production.
- Render has `OPENAI_MODEL=gpt-5.6-luna` and `OPENAI_EMBEDDING_MODEL=text-embedding-3-small`; the former misnamed `EMBEDDING_MODEL` key has been removed.
- The Agent Chat smoke request completed and returned a coherent answer from the user's memories. Follow-up client QA is deferred to Stage 5 by user decision because the component removes inline citation markers and does not render the returned citation metadata; the same page also displayed a zero-memory list while the agent found five memories.
- The Morgan 1.12.0, node-cron 4.6.0, and Moment 2.30.1 runtime group is deployed at `22d7d09`. Render verified container startup, MongoDB connectivity, Morgan health-request logging, and successful registration of the 08:00 Europe/London reminder task without manually firing it.
- Helmet 8.3.0 and CORS 2.8.6 are deployed at `288ef12`. Production verification confirmed `/health`, the allowed-origin and credential headers, omission of CORS permission for an unknown origin, Helmet's CSP and one-year HSTS header, and removal of `X-Powered-By`.
- bcryptjs 3.0.3 is deployed at `2273bfd`. The container and `/health` endpoint are healthy, and focused tests confirm existing `$2a$` hashes remain valid, new `$2b$` hashes retain 12 rounds, and new passwords cannot exceed bcrypt's 72-byte input limit. A credentialed production login was not independently performed for this dependency phase.
- dotenv 17.4.2 is deployed at `fb17c0f`. Render startup was clean, contained no dotenv injection messages, connected to MongoDB normally, and passed internal and public health checks.
- Google Auth Library 11.0.2 is deployed at `c34bce8`. Render starts normally, `/health` passes, and a deliberately invalid Google token returns HTTP 401. After correcting the ignored local API configuration, the user completed an ordinary Google sign-in successfully, closing the end-to-end QA check.
- The current production-install audit reports zero known vulnerabilities after refreshing MongoDB's compatible optional Socks dependency to 2.8.9.
- Atlas backup and access hardening remain deliberately deferred for their own controlled phase.
- The deployed client remains the existing React 18 build. Locally, the Vite migration is committed at `f93af6f`, React 19/Router 7 at `7f7ae64`, and the Sass-module cleanup at `f9e152a`; none has been pushed. The visual redesign is now complete locally as an uncommitted client-only slice. Tests, production build, authenticated desktop and mobile Memories checks, populated cards, Create modal, dashboard/content views, and browser-console review pass; client deployment remains intentionally deferred while changes are batched.
- Following user review, the redesign now has a strict four-colour lock: off-black, very dark blue, orange, and white. Every surface, border, muted label, semantic status, control state, and rating uses one of those four anchors or a calculated shade between them. The rejected cream/body background and the later circus-like collection of yellow, pink, green, gray, and blue accents have been removed. Header navigation now uses high-contrast white text. The user's fieldset wrapper remains the signature framing device across forms, memory cards, profile panels, and content sections. The Memories page now uses a compact control deck and responsive three/two/one-column masonry flow so image-heavy cards no longer create empty grid-row gaps. Its standard search and AI query are matching 175px labelled fieldsets in a two-column desktop row, collapsing to one column below 761px. Both use identical 46px inputs and aligned divider spacing. The search fieldset uses its available depth for a semantic `Memory status` fieldset containing the two larger active/completed metrics. The AI fieldset uses the natural label `Ask AI` and incorporates a `Due date` fieldset plus a bottom-aligned 54px Create button beneath its input. Latest and Soonest anchor to opposite sides of their available fieldset. Application behavior, routes, navigation labels, and form structure remain unchanged. Memory priority stars render in a dedicated horizontal inline-flex group instead of becoming separate children of the card's vertical flex layout.
- The user-specific Beta marker has been restored as a fixed orange top-left corner ribbon rotated at minus 45 degrees. It retains the approved palette, clears the first navigation control, ignores pointer input, and stays visible with a reduced footprint below 640px instead of disappearing. Desktop browser QA confirms the ribbon geometry and navigation clearance. The responsive CSS compiled successfully; the claimed browser tab did not accept the temporary viewport override, so the narrow variant remains for user visual confirmation. The client suite passes with 13 files and 22 tests, the Vite production build passes with 523 modules, and `git diff --check` passes.
- A second local theme-library review has been completed against the supplied design-system catalog and preview assets. `Construct - Spatial Engine` is the preferred primary reference because its compact dark workbench, nested operational panels, 8px geometry, and restrained technical hierarchy suit the internal application. `Mood Tracker Analytics` is useful only as a secondary reference for metric emphasis and selected orange states; its large mobile shells, 36px radii, and all-monospace body copy should not be copied. `Pricing Models` is a useful restraint reference, while the cinematic and WebGL-led themes are unsuitable for the daily-use client. The proposed next visual slice is a Memories-page workbench that keeps the four-colour palette and fieldset identity, unifies search and AI into a clearer command area, gives each memory a predictable metadata/content/action hierarchy, and retains the responsive masonry behavior for image-heavy records.
- The `Construct - Spatial Engine` Memories-page refinement is now implemented locally. A compact outer workbench introduces a clear page heading and total-memory indicator, with a 5/7 command grid above 1100px, equal query columns on tablet, and stacked panels below 761px. Query panels use scoped 8px geometry, flat dark-blue surfaces, restrained borders, and full-width equal Latest/Soonest buttons inside the `Filter due date` fieldset. The primary action is labelled `Create memory` and remains on one line down to 320px without overflow. The completed-memory status metric is now a semantic link to `#/user-admin`, where the existing dashboard renders the user's completed-memory cards. Completed cards label their checked state as `Unmark as complete`; active cards retain `Mark as complete`. A shared completion-feedback component gives state-specific success or error notifications on both the Memories and dashboard routes. The active-memory library has its own concise result header. Memory cards retain semantic fieldsets and masonry behavior while adding an unboxed priority row, a consolidated Memory controls fieldset, flatter image treatment, restrained card elevation, and a predictable hierarchy. Their visible fieldset title legends render in uppercase for consistency, while stored title values and accessible headings retain their original casing. When present, the rounded memory image sits directly beneath the main memory note. Created and Updated metadata follows as an unboxed text row, then Priority and the Memory controls fieldset. That fieldset contains the due-date and completion controls plus Edit, image upload/change, voice, and Delete on an unboxed action row separated by a single divider. The tag removal control and enlarged-image trigger are native labelled buttons, memory articles retain navigable heading names alongside their visible legends, count announcements no longer overlap, and shared buttons forward Agent Chat accessibility attributes. Desktop, 390px, and 320px browser checks pass without horizontal overflow or console warnings; Vitest, the Vite production build, `git diff --check`, the four-colour source audit, and the visible-source dash scan pass.
- The local dashboard now uses the accepted Memories workspace as its visual standard. `Dashboard workspace`, `Account overview`, and, when populated, `Completed memories` form compact shell sections with matching header rules, spacing, 8px geometry, dark surfaces, fieldset legends, and responsive behavior. The completed-memory section is omitted entirely when its count is zero. Account editing, profile image controls, memory metrics, account details, status, dates, and the administrator user selector have clear information hierarchy and consistent wording; the profile and editing fieldsets are labelled `Admin Profile Image` and `Edit Details`. Bare clickable profile icons and account values are now labelled buttons. Directly refreshing `#/user-admin` now reloads the authenticated user's details with the existing read-only request instead of leaving the account area blank. The expanded administrator selector now divides data into professional Identity, Access controls, and Activity cards with consistent label/value grids, readable Yes/No statuses, labelled toggles, and a black metric surface inside Memory summary. The two access controls use equal-width columns and collapse cleanly on narrow screens. The administrator selector's expanded state was browser-checked without changing user settings. Five Vitest files with eight tests, the production build, `git diff --check`, desktop browser QA, and a 390px responsive check pass.
- The local Memories modal system now follows the same workbench standard as the accepted page. Create and Edit use matching `Memory details` and `Reminder` fieldsets, labelled controls, concise supporting text, a consistent responsive grid, and clear primary actions. Delete uses a compact permanent-action confirmation, while image preview uses a dedicated media-sized shell with an accessible close control. The shared modal supplies dialog semantics, Escape handling, background scroll locking, focus restoration, consistent shell geometry, and compact/standard/media sizes. The React Datepicker surface, including its time input, now follows the four-colour theme. A collision with the global sticky-header rule was found during browser QA and removed by using scoped modal heading containers. Desktop checks of Create, Edit, Delete, the date picker, and image preview pass without mutating data; a 390px Create check confirms a single-column, internally scrollable dialog with no horizontal overflow. Six Vitest files with ten tests, the production build, `git diff --check`, the palette audit, and the visible-source dash scan pass.
- The local Contact route now follows the accepted workbench standard. A compact `Contact workspace` shell contains a focused two-panel layout: `Before you send` guidance and a structured `Send an enquiry` form. The former duplicated full About page has been removed from Contact while the independent About route remains unchanged. Name and Email share a balanced row, Message has an explicit label and live minimum-length guidance, and the primary action retains a stable `Send enquiry` label when disabled. Native required/minimum/description attributes now pass through the shared textarea component. The existing Redux/API submission flow is unchanged, and the success sentence and nine-character validation wording are corrected. Desktop and 390px browser checks pass without horizontal overflow; the mobile layout places the form first and guidance second. Seven Vitest files with eleven tests, the production build, `git diff --check`, palette scan, and visible-source dash scan pass.
- The remaining public entry surfaces now use the same documented workbench language. Home has a compact `Corporate memory` shell, paired Purpose/Memory model fieldsets, clear account/About actions, and a two-column knowledge-source library. About uses purposeful product, onboarding, AI, technology, protection, and Contact fieldsets instead of one long text slab. Both collapse to one column without horizontal overflow and keep the four-colour, 8px, fieldset-led system.
- The account and recovery surfaces now form one consistent `Account workspace`. Semantic Sign in, Create account, and Reset password buttons replace clickable text divs and expose their selected state. Login, registration, reset-link request, and token-based password reset share the same labelled fieldsets, concise headers, dark inputs, stable action wording, responsive grids, and full-width phone actions. Browser autofill is themed dark instead of rendering white credential fields. Existing Redux action payloads, Google login, routes, and authentication boundaries remain intact. Registration success now supplies its required reset callback, recovery copy is corrected, and password-reset redirection occurs only after API success with timer cleanup. Desktop and 390px browser checks covered all non-submitting states; ten Vitest files with fifteen tests and the production build pass.
- A full form-validation audit is now complete locally across public authentication and recovery, Contact, memory create/edit, account-detail editing, Agent Chat, and memory/profile image uploads. Shared client helpers now keep account, password, memory, priority, and image rules consistent. New passwords use the existing backward-compatible 6-to-72-character range plus uppercase, lowercase, number, and special-character requirements; sign-in checks only the minimum needed for existing credentials. Whitespace-only memory/contact/agent input is rejected, enabled reminders require a selected date, priorities remain within 1-5, AI filters accept only documented priority values, and image controls accept only JPG/PNG files no larger than 5 MB before preview. Inline errors are associated with their inputs, the password visibility control is keyboard accessible, and action labels remain stable while disabled. The API independently enforces the corresponding account, Contact, memory, and Agent Chat rules without changing existing stored records. Client tests pass with 12 files and 21 tests, API tests pass with 37 tests, the Vite production build passes with 523 modules, read-only browser validation QA passes, and a fresh-page console check is clear.
- Authenticated users now have a compact Dashboard accent-colour selector. Orange remains the initial default, with Yellow, Green, and Indian Red alternatives. The selection replaces only the existing accent token family, including its hover, soft, deep, focus, status, and rating derivatives; black, dark-blue, and white surfaces, typography, spacing, and fieldset structure remain fixed. Choices apply immediately throughout the application and persist per account in local browser storage. The most recently selected accent is also retained on public pages after logout, while signing into an account with its own saved choice restores that account's preference. Account identity is recovered from existing authenticated details or the JWT identifier. The selector is keyboard operable, reports its expanded and selected states, and closes with Escape. All four accent/button-text combinations exceed a 4.5:1 contrast ratio. Focused preference and selector tests pass, the full client suite passes with 15 files and 26 tests, and the Vite production build passes with 526 modules. Authenticated browser visual QA remains for the user because the currently open local session is signed out.
- All four API email flows now use one email-safe visual system that mirrors the accepted application: off-black canvas, dark-blue workbench card, white content, fixed orange accents, compact technical labels, 8px panels, and fieldset-like information blocks. Registration confirmation, password reset, contact acknowledgement, and seven-day reminders each have purpose-specific hierarchy and action wording while sharing the same branded shell and plain-text fallback. Table layout and inline styles avoid reliance on client-side CSS, JavaScript, external fonts, or remote images. Customer names, messages, and memory titles are escaped before HTML interpolation, multiline contact messages retain their formatting, and action links reject non-HTTP(S) protocols. Recipients, BCC handling, subjects, confirmation/reset tokens, reminder scheduling, and reminder state updates are unchanged. A controlled production Contact request returned success, and Render recorded `POST /api/contact-form` with HTTP 200 in 825 ms. Recipient QA then found that Gmail rendered the navy as pale blue. The local correction retains exact `#080b10`, `#101d2f`, `#ff6200`, and `#f7f9fc` values while adding legacy `bgcolor` attributes, same-colour gradient fallbacks, and a light-only colour-scheme declaration to resist client recolouring. The corrected browser preview matches the application, all 41 API tests pass, and production/Gmail retesting awaits commit and deployment approval.
- The Render GitHub App installation for the personal `MeGallin` account now includes `MeGallin/new-corporate-memory-render`. Deployment `dep-dac4d5ijobas73cg1qq0` successfully cloned and checked out email-template commit `02c494b`, built the existing Dockerfile, started the Node API, connected to MongoDB, enabled the reminder cron, and became live in 43.1 seconds. Render recorded two successful `GET /health` responses with HTTP 200. The earlier clone failure is resolved; no `GADev-equus` repository permission was changed.

### Next recommended action

Review and commit the local Gmail colour-rendering correction, then deploy it to Render and send one final controlled Contact message to verify the exact dark-blue palette in Gmail. The change affects presentation markup only; recipients, email content, application data, and transport behaviour remain unchanged. Production data changes remain out of scope.

## Stage 1 — Local Docker parity

- [x] Add an API Dockerfile that installs locked production dependencies and runs as a non-root user.
- [x] Exclude local secrets and development artifacts from the Docker build context.
- [x] Add a database-aware `/health` endpoint.
- [x] Add graceful HTTP, cron, and MongoDB shutdown handling.
- [x] Add a disposable MongoDB 8 local Compose environment.
- [x] Add a repeatable health smoke test.
- [ ] Build the image and run the smoke test locally. Render has successfully built and run the same Dockerfile; the local check remains blocked only by the Docker Desktop host fault.
- [x] Review the resulting source changes before production preparation.

## Stage 2 — Production safety and critical security

- [x] Verify Google identity tokens cryptographically using an API-side Google client ID.
- [x] Require authentication and ownership checks for memory-image uploads.
- [x] Derive profile-image ownership solely from the authenticated user.
- [x] Correct authentication middleware error propagation.
- [x] Add a 5 MB image limit, strict JPEG/PNG checks, and reliable temporary-file cleanup.
- [x] Add simple rate limits to public authentication and password-reset routes.
- [x] Limit the administrative user endpoint to required user fields and memory ownership references; do not load or return memory content.
- [x] Add focused built-in tests for bearer-token parsing, verified Google payloads, and ownership comparison.
- [x] Add focused route-level tests for rejected authentication, admin response exposure, rejected upload ownership, and authentication/recovery rate limits.

## Stage 3 — Backup and Render Docker conversion

- [ ] Create a timestamped `mongodump` archive from Atlas without changing database data.
- [ ] Verify the dump completed successfully and store it outside the repositories.
- [x] Record current Render settings and preserve all environment values.
- [x] Switch the existing Render service to its Docker runtime.
- [x] Configure `/health` as the Render health-check path.
- [x] Run production smoke checks and review Render logs.

## Stage 4 — API dependency modernization

- [x] Upgrade Mongoose 6 to Mongoose 8 and resolve deprecated connection and middleware behavior.
- [x] Deploy Mongoose 9.9.4. Production is healthy at `8bc7239`; create, edit, read, and delete checks passed, and image behavior had already been verified.
- [x] Upgrade Cloudinary and Multer in an isolated upload-focused change set to remove the legacy `vm2` path and Multer 1.x warnings.
- [x] Upgrade JSON Web Token 8 to 9 and pin application token verification to HS256.
- [x] Upgrade Express in its own controlled change set.
- [x] Upgrade Nodemailer and harden the SMTP transport.
- [x] Consolidate registration, password-reset, contact, and reminder emails into one branded, email-safe template system with escaped dynamic content and plain-text fallbacks.
- [x] Upgrade LangChain/OpenAI and move Agent Chat to the cost-sensitive GPT-5.6 Luna model; deployed at `1cdb0be` and verified with an authenticated production request.
- [x] Complete the final API dependency deployment. Morgan 1.12.0, node-cron 4.6.0, Moment 2.30.1, Socks 2.8.9, Helmet 8.3.0, CORS 2.8.6, bcryptjs 3.0.3, dotenv 17.4.2, Google Auth Library 11.0.2, and Mongoose 9.9.4 are complete in production.
- [x] Re-run the production-only security audit and resolve the currently reported reachable vulnerabilities; the local audit now reports zero findings.
- [ ] Move scheduled jobs out of the web process before adding more API instances.
- [ ] Avoid re-embedding every memory on every Agent Chat request.

## Stage 5 — Client modernization

- [ ] Deploy the memory-delete confirmation correction. The client-only implementation is committed locally at `ef7912e` but not pushed or deployed. The first click opens an application modal, Cancel performs no dispatch, and only a separate `Delete memory` button can invoke deletion. Focused Jest coverage passes, browser QA confirmed the modal/Cancel path without deleting a record, and the user confirmed the corrected flow works locally.
- [ ] Render Agent Chat citation metadata instead of discarding inline citation markers, and investigate the observed memory-list/count mismatch.
- [x] Replace the obsolete Create React App placeholder test with focused behavior coverage and migrate the runner to Vitest 4.1.11; the focused test passes after a clean install.
- [x] Migrate Create React App 5 to Vite 8.2.2 locally while retaining React 18, hash routing, port 3000, ignored mode-specific environment files, and the existing `build/` output directory. Both development and built-preview browser smoke checks passed without console errors; committed locally at `f93af6f`, with deployment pending.
- [x] Replace legacy Sass `@import`, global colour helpers, and duplicated stylesheet imports locally. Shared design tokens now live in a CSS-free Sass module, global styles load once, deprecated colour calls use `sass:color`, and the unused mixin file is removed. Tests and a warning-free production build pass; committed locally at `f9e152a`, with push and deployment pending.
- [x] Select and refine the visual redesign direction from the supplied references. The final user-directed system is constrained to four colour anchors: off-black, very dark blue, orange, and white. Ray of Light supplies the orange-on-black identity while Nexcore supplies the compact dashboard hierarchy and nested dark-blue surfaces. Avoid unrelated semantic colours, marketing-scale headings, WebGL/particle effects, and excessive animation.
- [x] Implement the visual redesign locally as a client-only slice. Central semantic tokens now derive every interface colour from the four approved anchors, while Space Grotesk and JetBrains Mono provide the typography. The shared system covers controls and states, application shell, high-contrast navigation, responsive Memories workspace, Agent Chat, image treatment, dashboard, forms, modal, footer, notices, and secondary views. The fieldset is the signature wrapper, including real fieldset/legend memory cards. The Memories page separates its two query modes into equal-width, equal-height desktop fieldsets: `Find a memory` retains live counts, while the more natural `Ask AI` fieldset incorporates the Due date segmented control and Create action beneath its input. All three legends share identical white typography and inset spacing; Due date and Create share the same 61px rendered height. The query layout collapses below 761px without horizontal overflow. Memory cards use natural heights and a responsive masonry flow with three columns above 1100px, two columns from 761-1100px, and one below 761px. Card media remains full-width above a compact action row ordered Edit, image action, voice action, Delete; the image and voice icons are now accessible 15/16px controls inside 34px buttons. Sort controls are keyboard-accessible buttons and priority stars are grouped horizontally. Focused Vitest, Vite production build, `git diff --check`, desktop/mobile browser QA, computed breakpoint verification, selected-sort interaction, modal review, header computed-style verification, and console review pass. User functional QA and explicit commit approval remain pending; proposed commit: `client: redesign application visual system`.
- [x] Refine the Memories page into the selected `Construct - Spatial Engine` workbench. The scoped slice adds the compact workbench and active-library headers, a wide-screen 5/7 query grid, flat 8px operational panels, full-width equal Latest/Soonest sort buttons inside a `Filter due date` fieldset, a `Create memory` action, 1.125rem main memory-note text, a rounded memory image directly beneath the note, an unboxed audit-date row, a vertically stacked unboxed Priority label and smaller star row, and a consolidated Memory controls fieldset containing state controls plus Edit, image, voice, and Delete actions. The four card-action positions use equal-width columns, eliminating the expanding middle gap; image change/delete controls share the image column when present, and narrow-card label padding is reduced below 421px. It also provides uppercase visible memory-card legends without changing stored or accessible title casing, restrained memory-card elevation, labelled tag-delete and image-view buttons, navigable memory headings, non-duplicated status announcements, and working Agent Chat disclosure attributes. It preserves the four-colour lock, existing behavior, component ownership, routes, fieldset signature, and three/two/one-column masonry. Vitest, production build, diff and palette checks, 1280px/390px/320px rendered checks, overflow checks, and console review pass; authenticated populated-card QA remains for the user.
- [x] Link the completed-memory status metric to the existing `#/user-admin` dashboard route. The link has a count-aware accessible name, keyboard focus treatment, and preserves the accepted status-panel appearance. Automated route coverage and a running-app browser check confirm the link opens the dashboard's Completed Memories section.
- [x] Make completion controls and notifications state-aware on both client routes. Completed cards say `Unmark as complete`, active cards say `Mark as complete`, and the Redux success state retains the requested boolean so the shared feedback component can distinguish marking from unmarking. Focused tests cover both messages and the completed-card dispatch, while read-only browser QA confirms the unmark control is visible on the dashboard without changing a memory.
- [x] Bring the dashboard route up to the accepted Memories workspace standard. Compact shell sections, consistent fieldset legends, an account-information grid, memory metrics, accessible edit/profile controls, conditional completed-memory output, two/one-column completed-card flow, and responsive breakpoints replace the former deeply nested and oversized layout. The completed-memory section is hidden when there are no completed records. Administrator selection and account details retain their existing behavior with clearer labels. Its expanded view uses Identity, Access controls, and Activity cards, consistent definition-list alignment, explicit Yes/No status values, accessible toggle names, and a black Memory summary metric surface. A direct-load hydration correction fetches missing user details for both ordinary and Google-authenticated sessions. Focused dashboard tests cover section output, direct-load hydration, and the zero-completed state; the full client suite, production build, diff check, authenticated desktop QA, expanded admin-panel QA, and 390px responsive review pass.
- [x] Carry the accepted workbench design through the public Contact route. The page now uses the same compact shell header, fieldset legends, four-colour surfaces, 8px geometry, spacing rhythm, and responsive rules as Memories and Dashboard. A focused guidance panel replaces duplicated About-page content beside the enquiry form. Inputs are explicitly labelled, the Message control exposes its minimum requirement, and the action retains meaningful wording in its disabled state. The original contact action and API request contract remain unchanged. Focused submission coverage, the full client suite, production build, desktop browser review, and 390px no-overflow review pass.
- [x] Carry the accepted workbench design through Home and About. Home now presents the purpose and model of corporate memory before a compact knowledge-source grid, with clear entry actions. About now separates product purpose, onboarding, AI behavior, technology, data protection, and Contact into consistent fieldsets. Both preserve their routes and core subject matter, use the four-colour/8px/fieldset system, and collapse to one column on phones without overflow.
- [x] Consolidate login, registration, forgotten-password, and password-reset surfaces into one responsive account-workspace system. The Forms route uses semantic mode buttons with selected-state attributes, a consistent access-guidance panel, dark autofill-safe controls, stable action labels, and a shared responsive form layout. Google login and existing Redux/API request contracts are preserved. Password-reset navigation now waits for successful completion and cleans up its timer. Focused workspace, mode-switching, and reset-payload tests pass alongside desktop and mobile browser QA of every non-submitting form state.
- [x] Restyle all Memories-route modals to the accepted workbench system. The shared modal shell now provides compact, standard, and media sizes; consistent black/dark-blue/orange/white styling; an always-visible labelled close control; dialog semantics; Escape dismissal; body-scroll locking; and focus restoration. Create and Edit share structured `Memory details` and `Reminder` fieldsets, real labels, matching spacing, responsive one/two-column control layouts, and clearer actions. Delete is a compact permanent-action confirmation, image preview uses the media shell, and the date picker is fully dark-themed. Browser QA covered every modal without submitting or deleting data, including 390px responsive behavior. Focused modal tests, the full client suite, the production build, diff/palette/dash checks, and manual accessibility inspection pass.
- [x] Align form validation across the client and API. Authentication, registration, password recovery/reset, Contact, memory create/edit, account-detail editing, Agent Chat, and image selection now apply matching field rules at the UI and request boundaries, expose specific inline feedback, preserve stable action labels, and retain API-side validation as the final authority. Existing login compatibility, authentication/ownership boundaries, stored records, and the 5 MB JPG/PNG upload contract remain intact.
- [x] Restore the Beta marker as the user's distinctive minus-45-degree orange corner ribbon, with non-blocking pointer behavior and a smaller responsive treatment.
- [x] Add an authenticated Dashboard accent-colour selector with Orange, Yellow, Green, and Indian Red variants. The runtime token layer changes the complete accent family without altering the accepted dark surfaces or fieldset-led layout, stores the choice per account, and retains the last selected accent on signed-out public pages in the same browser.
- [x] Upgrade React Router 6 to 7 locally with the React 19 dependency phase. React Router 7.18.3 retains the existing declarative `HashRouter` behavior and removes the two remaining router audit findings; authenticated navigation QA passed, with commit approval and deployment pending.
- [x] Upgrade React 18 to React 19 locally and update incompatible dependencies. React 19.2.8, React DOM 19.2.8, React Redux 9.3.0, Redux 5.0.1, Redux Thunk 3.1.0, React Datepicker 9.1.0, current Testing Library packages, and the maintained Redux DevTools extension install cleanly with zero audit findings. Function-component `defaultProps` were converted to default parameters. Tests, build, browser smoke checks, ordinary and Google login, authenticated navigation, and create/edit date-picker QA pass; commit approval and deployment remain pending.
- [ ] Reassess browser token storage and authentication handling.
- [ ] Build and verify the static production client before updating Trilogy hosting.

## Stage 6 — Atlas hardening

- [ ] Create a least-privilege database user for the application.
- [ ] Restrict the Atlas IP access list to the required Render and administrative source ranges.
- [ ] Decide whether to retain manual dumps or upgrade Atlas for managed backups.
- [ ] Do not combine Atlas access changes with the initial Render runtime conversion.
